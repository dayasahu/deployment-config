

1. Configuration Management 

Create a config map and deploy using Kcustomization and overlay .

Volume (config map) - > volume mount for each service 
 
Change in properties file deploy config map - > refresh the service using actuator endpoint ->


2. API Gateway Setup 

what is solves - 

 whitelisting issues 
 single entry point 
 synamic routing 
 centrlize token validation 
 API security 
 rate limit 
 

Server - Service communcation 

 service A nginx - > service A k8 POD -> Nginx token validation -> logic -> Service B nginx  -> service B token validation -> logic -> return reponse 
 
 nginx -> gateway route -> token validation -> service A - > service B ( using clusterIP )
 
 
 3. Open telemtry 
 
 generate trace id and span id 
 

  4. k8 service discovery 
  
  department  → Kubernetes Service
             → DNS resolves to ClusterIP
             → Load balances to department Pods
